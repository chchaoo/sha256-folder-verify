# 更新日志

[English](CHANGELOG.md) · **简体中文**

仓库根目录下的 `verify.bat` 始终是最新版本。以前的每个版本都保存在 [`history/`](history/) 目录中，仅作留档，不建议使用。

## v14 · 2026-09-24 · 最新

- **只在一边存在的文件不再读取。** 副本里缺少或多出的文件，不管内容是什么，结论都一样，所以直接报告。只有两边都有、大小相同的文件才计算哈希。复制到一半中断时，缺少的那一半完全不用读。
- 开始读取前的 `NOT READ` 改为两行：跳过的总量，以及每种原因各有多少个文件。

## v13

- **先比大小。** 两边都有但大小不同的文件不可能一致，直接报 `[SIZE DIFFERS]`，两边都不读。结果表新增 `Different size` 一项。
- 被跳过的文件不计入进度条和剩余时间，进度条照样精确走到 100%。
- 扫描时读不到大小的文件不走这条捷径，避免把扫描错误误报成"大小不同"。

## v12

- **单文件进度。** 每一边新增一行 `File`，显示当前正在读的文件的百分比、已读字节、文件大小和剩余时间。50 GB 的大文件也能看到进度在走，不会几分钟卡在同一个数字上。
- 读取线程把"当前文件 + 已读字节"作为一个整体发布，屏幕上的文件名和进度永远属于同一个文件，切换到下一个文件的瞬间也不会错配。

## v11

- 剩余时间估算更准。每 MiB 的读取时间直接取最近 3 秒实测的速度（和屏幕上显示的速度一致）；每个文件的固定开销只看最近约 15 秒的文件（v10 是 2 分钟），并且计算速度前先扣除窗口内的固定开销，避免重复计算。

## v10

- **剩余时间估算（ETA）**，按"每个文件的固定开销 + 每 MiB 的读取时间"建模。每一边显示实时速度。
- 通过 WMI 按**物理磁盘**判断两个文件夹是否在同一块盘上，同一块盘的两个分区不再并行读。支持 SUBST 虚拟盘、跨盘卷和网络服务器。

## v9

- 进度文件名固定为 `verify_resume.txt`，不再随 `.bat` 的文件名变化，改名或换版本后仍然可以续跑。

## v8

- 修正进度块中空行的显示问题。

## v7

- **取消多线程并行。** 每个文件夹只用一个线程，一个文件读完再读下一个，这正是机械硬盘需要的读法。两块盘同时读，同一块盘先后读。
- 进度块里原件和副本之间加空行分隔；修正进度条的报错。

## v6

- 四根进度条：原件和副本各有"文件数"和"数据量"两根。

## v5

- 多个工作线程并行计算哈希。
- **断点续跑**：进度保存在 `.bat` 旁边，中断后可以接着跑。

## v4

- **不再往磁盘写任何东西。** 引擎直接在内存里编译执行，不再释放到 `%TEMP%`（v3 用 X 关闭窗口时会残留临时文件）。
- 支持循环：跑完按任意键开始下一次校验。

## v3

- **改用 PowerShell 引擎重写，结果从此正确。** v2 系列是纯批处理，处理不了带 `%` `!` `^` 或以 `;` 开头的文件名，看不到隐藏文件和空文件夹，超长路径会出错，还会把 `certutil` 的错误信息当成哈希值存下来。从 v3 起 `.bat` 只作为启动器（这一版会把引擎释放到 `%TEMP%` 再运行）。

## v2 系列

- **v2**：改为把文件夹拖进窗口来指定，不再用对话框选择。
- **v2-1**：进度显示不再闪烁。
- **v2-2**：显示每个文件的大小。
- **v2-3**：两根进度条，分别按文件数和数据量。
- **v2-4**：进度更新更及时。
- **v2-5**：进度条对齐到固定的网格位置。

## v1

- 第一版。用 Windows 的文件夹对话框选择原件和副本，用 `Get-FileHash` 生成两份排好序的哈希清单，再用 `fc` 比较。
