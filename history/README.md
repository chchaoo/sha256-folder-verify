# history/

Every earlier version of the tool, kept for reference. **Use [`../verify.bat`](../verify.bat) instead.** It is the latest version, and several of these have known problems that later versions fixed (v1 and the v2 series can report wrong results). What changed in each one is in the [changelog](../CHANGELOG.md).

这里保存着工具以前的每一个版本，仅作留档。**请使用根目录的 [`verify.bat`](../verify.bat)**，那是最新版本；这里的不少版本有后来才修复的问题（v1 和 v2 系列可能给出错误的结果）。每个版本改了什么见[更新日志](../CHANGELOG.zh-CN.md)。
